## Packages
date-fns | Essential for date formatting and manipulation
framer-motion | Smooth animations for page transitions and dialogs
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
Integration with backend uses standard REST endpoints defined in shared/routes.
Images are handled via URL input (Unsplash or external) for this version.
